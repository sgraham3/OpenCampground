<%= render :partial => 'setup/buttons' %>
<p>
<%= button_to 'Finished', setup_creditcards_path, :method => :get %>
<div id="container" style="overflow: scroll; height: <%= (@option.disp_rows*2).to_s %>em">
 <ul id="creditcards"> 
  <% @creditcards.each do |s| %>
      <li id="creditcard_<%=s.id%>"><%= s.name %></li>
  <% end %>
</ul>
</p>
<%= javascript_tag "Position.includeScrollOffsets = true" %>
<%= sortable_element :creditcards, :scroll => "'container'", :url => resort_setup_creditcards_path %>
