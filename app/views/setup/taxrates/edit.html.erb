<% form_tag setup_taxrate_path(@taxrate), :method => :put do %>
  <%= render :partial => 'form' %>
  <p>
  <%= submit_tag 'Save' %>
  </p>
<% end %>
<%= button_to 'Cancel', setup_taxrates_path, :method => :get %>
